{% extends 'base.html.twig' %}

{% block libelle %} Nos Articles {% endblock %}

{% block body %}
<div class="container">

    <h1 class="my-5"> ARTICLES - TPSYm  </h1>

    
        
    
    <table class="table table-hover table-striped">
        <thead>
            <tr class="d-flex">
                <th scope="col" class="col-md-1" style='background-color:#a50000; color:#000000'>Id</th>
                <th scope="col" class="col-md-5" style='background-color:#a50000; color:#000000'>Libelle</th>
                <th scope="col" class="col-md-3" style='background-color:#a50000; color:#000000'>Prix</th>
                <th scope="col" class="col-md-3" style='background-color:#a50000; color:#000000'>Actions</th>
                <th><button><a href="{{ path('ads_create')}}"><i class="fas fa-plus-square"></a></button></th>
                
            </tr>
        </thead>
        <tbody>
            {% for ad in list %}
            <tr class='d-flex'>
            <td class='col-md-1' style='background-color:#c9956c; color:#202020'>{{ad.id}}</td>
            <td class='col-md-5' style='background-color:#c9956c; color:#202020'>{{ad.libelle}}</td>
            <td class='col-md-3' style='background-color:#c9956c; color:#202020'>{{ad.prix}}</td>
            <td class='col-md-3' style='background-color:#c9956c; color:#202020'> <a href="{{ path('ads_edit', {'id' : ad.id}) }}" class="btn btn-secondary"><i class='fas fa-pencil-alt'></i></a><a href="{{ path('ads_delete', {'id' : ad.id})}}" onclick="return confirm(`Êtes vous sur de vouloir supprimer l'annonce : {{ad.libelle}} ? `)" class="btn btn-warning"><i class='fas fa-trash-alt'></i></a></td> 
            </tr>
            {% endfor %}
    
         </tbody> 
    </table>
</div>
{% endblock %}
